language: php

php:
    - 5.4
    - 5.5
    - 5.6
    - hhvm

matrix:
    allow_failures:
        - php: hhvm

before_script:
  - mysql -e 'create database phpauthtest;'
  - mysql -u root -p phpauthtest < database.sql

script:
    - phpunit

notifications:
    email: false
